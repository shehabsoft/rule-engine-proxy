apply plugin: "war"

bootWar {
    mainClassName = "com.emu.rule.proxy.RuleEngineProxyApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
